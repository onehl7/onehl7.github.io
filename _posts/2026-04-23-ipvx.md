---
layout: post
title: "IPvX: IPv4 and IPv6"
date: 2026-04-23 18:00:00 +0000
category: tech
excerpt: "A look at the IPv6 adoption and a high-level comparison of IPv4 and IPv6."
---

### The Current State: IPv6 Adoption

Let's look at the real-world transition happening right now. Google provides an excellent real-time view of this progress, and APNIC lab as well.

Below is a recent snapshot (April 2026) of the global IPv6 adoption rate from Google. 

![Google IPv6 Adoption Preview](/assets/images/google-ipv6-adoption-preview.png)
*A visual preview of global IPv6 adoption trends- Google*

For the most current, live data, visit Google's public dashboard directly or APNIC lab IPv6 measurement maps.


**View the live statistics here:**
[**Google IPv6 Adoption Statistics**](https://www.google.com/intl/en/ipv6/statistics.html#tab=ipv6-adoption){:target="_blank" rel="noopener"}
Or
[**APNIC IPv6 Measurement Maps**](https://stats.labs.apnic.net/ipv6/){:target="_blank" rel="noopener"}

## Comparing IPv4 and IPv6

The table below provides a high-level comparison of the key features of IPv4 and IPv6.

<style>
  table {
    width: 100%;
    border-collapse: collapse;
    margin: 25px 0;
    font-size: 0.9em;
    font-family: 'Inter', sans-serif;
    background-color: var(--table-bg);
    color: var(--text-color);
    table-layout: fixed; /* Enforce even column widths */
  }
  table thead tr {
    background-color: var(--table-header-bg);
    color: var(--table-header-text);
    text-align: left;
    border-bottom: 2px solid var(--table-header-border);
  }
  table th,
  table td {
    padding: 12px 15px;
    border: 1px solid var(--table-border);
    word-wrap: break-word; /* Wrap long words */
    vertical-align: top; /* Align content to the top */
  }
  table th:first-child,
  table td:first-child {
    width: 20%; /* Give less space to the 'Feature' column */
  }
  table th:nth-child(2),
  table td:nth-child(2) {
    width: 40%; /* IPv4 column */
  }
  table th:nth-child(3),
  table td:nth-child(3) {
    width: 40%; /* IPv6 column */
  }
  table tbody tr {
    border-bottom: 1px solid var(--table-border);
  }
  table tbody tr:nth-of-type(even) {
    background-color: var(--table-alt-bg);
  }
</style>

<table>
  <thead>
    <tr>
      {% for header in site.data.ip_versions.headers %}
        <th>{{ header }}</th>
      {% endfor %}
    </tr>
  </thead>
  <tbody>
    {% for row in site.data.ip_versions.rows %}
      <tr>
        {% for cell in row %}
          <td>{{ cell | markdownify }}</td>
        {% endfor %}
      </tr>
    {% endfor %}
  </tbody>
</table>